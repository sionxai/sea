"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function LoginForm() { const router = useRouter(); const [email,setEmail] = useState("user@ocean.local"), [password,setPassword] = useState("ocean2026!"), [error,setError] = useState(""), [sending,setSending] = useState(false); async function submit(e: React.FormEvent) { e.preventDefault(); setSending(true); setError(""); const response = await fetch("/api/auth/login", {method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})}); const data = await response.json() as {role?:string;message?:string}; setSending(false); if (!response.ok) return setError(data.message ?? "로그인하지 못했습니다."); router.push(data.role === "admin" ? "/admin" : "/orders"); router.refresh(); } return <form className="form panel" onSubmit={submit}><div className="notice">체험 계정: <b>user@ocean.local</b> / <b>ocean2026!</b><br/>관리자: <b>admin@ocean.local</b> / <b>ocean2026!</b></div><label>이메일<input className="field" type="email" value={email} onChange={e => setEmail(e.target.value)} required/></label><label>비밀번호<input className="field" type="password" value={password} onChange={e => setPassword(e.target.value)} required/></label>{error && <div className="error">{error}</div>}<button className="button" disabled={sending}>{sending ? "로그인 중…" : "로그인"}</button><p className="muted">이 화면은 로컬 MVP용 이메일 로그인이며, 실제 운영 전에는 OAuth와 비밀키 환경설정이 필요합니다.</p></form>; }
