import Image from "next/image";
import Link from "next/link";
import { CheckoutForm } from "@/components/checkout-form";
export default async function CheckoutPage({ searchParams }: { searchParams: Promise<{ product?: string }> }) { const productId = Number((await searchParams).product); return <main className="subpage"><section className="shell checkout-layout"><aside className="checkout-aside"><div className="checkout-image"><Image src="/images/order-support.png" alt="배송을 준비한 바다숲 키트 패키지" fill priority sizes="(max-width: 760px) 100vw, 46vw"/></div><p className="eyebrow">RESPONSIBLY PACKED</p><h1>바다숲으로 가는<br/>마지막 준비</h1><p>플라스틱 포장을 줄이고 키트에 꼭 필요한 구성만 담았습니다.</p><ul><li>배송비 포함</li><li>비회원 주문 가능</li><li>결제 후 주문 조회 가능</li></ul></aside><section className="checkout-content"><p className="eyebrow">CHECKOUT</p><h2>주문 정보 입력</h2>{Number.isInteger(productId) && productId > 0 ? <CheckoutForm productId={productId}/> : <div className="state"><p>선택한 상품이 없습니다.</p><Link className="button" href="/products">상품 목록으로 돌아가기</Link></div>}</section></section></main>; }
