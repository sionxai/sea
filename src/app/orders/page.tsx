import Image from "next/image";
import { Orders } from "@/components/orders";
import { getSession } from "@/lib/session";
export default async function OrdersPage() { const session = await getSession(); return <main className="subpage"><section className="orders-hero"><div className="shell orders-hero-inner"><div><p className="eyebrow">MY ORDERS</p><h1>바다숲 키트가<br/>어디쯤 왔을까요?</h1><p>결제 완료부터 배송 완료까지 주문 상태를 한눈에 확인할 수 있습니다.</p></div><div className="orders-image"><Image src="/images/order-support.png" alt="배송 준비가 완료된 바다숲 키트" fill priority sizes="(max-width: 760px) 100vw, 46vw"/></div></div></section><section className="shell orders-section"><div className="section-head"><div><p className="eyebrow">ORDER HISTORY</p><h2>주문·배송 내역</h2></div></div><Orders authenticated={Boolean(session)}/></section></main>; }
