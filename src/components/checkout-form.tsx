"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function CheckoutForm({ productId }: { productId: number }) { const router = useRouter(); const [recipient, setRecipient] = useState(""), [address, setAddress] = useState(""), [quantity, setQuantity] = useState(1), [sending, setSending] = useState(false), [message, setMessage] = useState(""); const valid = recipient.trim().length > 1 && address.trim().length > 5 && quantity > 0; async function submit(event: React.FormEvent) { event.preventDefault(); if (!valid) return; setSending(true); setMessage(""); const response = await fetch("/api/orders", { method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({ productId, quantity, recipient, address }) }); const data = await response.json() as { id?: number; message?: string }; setSending(false); if (!response.ok) { setMessage(data.message ?? "주문을 처리하지 못했습니다."); return; } router.push(`/orders?created=${data.id}`); } return <form className="checkout-form" onSubmit={submit}><div className="notice">실제 청구 없이 ‘테스트 결제 완료’ 상태로 안전하게 저장됩니다.</div><div className="form-fields"><label>받는 분<input className="field" value={recipient} onChange={(e) => setRecipient(e.target.value)} required placeholder="이름을 입력해 주세요"/></label><label>배송지<input className="field" value={address} onChange={(e) => setAddress(e.target.value)} required placeholder="시·군·구부터 상세 주소까지 입력"/></label><label>수량<input className="field" min="1" type="number" value={quantity} onChange={(e) => setQuantity(Number(e.target.value))} required/></label></div>{message && <div className="error">{message}{message.includes("로그인") && <> <a href="/login" className="inline-link">로그인하기</a></>}</div>}<button className="button checkout-submit" disabled={!valid || sending}>{sending ? "결제 처리 중…" : "테스트 결제 완료하기"}</button><p className="privacy-note">주문 버튼을 누르면 입력한 배송 정보가 로컬 DB에 저장됩니다.</p></form>; }
