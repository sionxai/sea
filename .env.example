# Required outside local development. Use at least 32 random characters.
SESSION_SECRET=

# Development only. Demo accounts are never seeded in production.
ENABLE_DEMO_ACCOUNTS=true
DEMO_ACCOUNT_PASSWORD=replace-for-local-demo
