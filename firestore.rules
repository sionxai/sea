rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // All application data is accessed through the server-only Admin SDK.
      allow read, write: if false;
    }
  }
}
